{% extends 'base.html.twig' %}

{% block title %}Post index{% endblock %}

{% block body %}
    <h1>Post index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Image</th>
                <th>Description</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for post in posts %}
            <tr>
                <td>{{ post.id }}</td>
                <td><img style="height:50px;width:50px;" src="{{ vich_uploader_asset(post, 'imageFile') }}" alt=""></td>
                <td>{{ post.description|raw }}</td>
                <td>Le {{ post.date|date(" d-m-Y à H:i:s") }}</td>
                <td>
                    <a href="{{ path('admin_post_show', {'id': post.id}) }}">show</a>
                    <a href="{{ path('admin_post_edit', {'id': post.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_post_new') }}">Create new</a>
{% endblock %}
